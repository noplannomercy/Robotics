# 역문서엔진 MVP (의존성 순서 현상 파악) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Oracle PL/SQL 패키지를 의존성 순서로 정렬해 기존 Robotics 생성 엔진에 순차 투입하고, 역문서 현상(통과율·빈약 지점)을 파악한다.

**Architecture:** 신규 코드는 서비스 위의 얇은 오케스트레이션 3개뿐 — (1) 위상 정렬기, (2) 순차 적재 드라이버(`POST /jobs` → 완료 폴링 → 다음), (3) 결과 요약. 기존 `to_reverse_doc`/엔드포인트는 그대로 재사용. 의존성 순서로 넣으므로 callee 컨텍스트는 각 패키지 처리 시점에 이미 LightRAG에 적재되어 있다.

**Tech Stack:** Python 3.11+, pytest, httpx(동기 Client), 기존 Robotics FastAPI 서비스.

**⚠️ 실행 환경 제약:** 실제 PL/SQL 소스·의존성 데이터는 **사내망 전용**(미반출). 본 plan의 코드는 이 PC에서 **mock/샘플로 TDD**하고, **1,800패키지 실적재는 사내망**에서 수행한다. 모든 단위 테스트는 실데이터 없이 통과 가능하도록 설계한다.

---

## File Structure

- `dependency_order.py` — (신규) 위상 정렬 순수 함수. 입력: 노드+엣지, 출력: 정렬 리스트+순환 노드. 외부 의존 없음.
- `ingest_driver.py` — (신규) 순차 적재 로직(`run_ingestion`, 순수·DI) + 얇은 HTTP 클라이언트(`HttpRoboticsClient`).
- `observation.py` — (신규) 적재 결과 요약(`summarize`). 통과율·카운트 집계.
- `docs/observations/TEMPLATE.md` — (신규) 현상 수동 기록 템플릿.
- `tests/test_dependency_order.py` / `tests/test_ingest_driver.py` / `tests/test_observation.py` — (신규) 단위 테스트.

기존 서비스 코드(`app.py`, `processor.py`, `validator.py` 등)는 **수정하지 않는다.** MVP는 서비스 위 오케스트레이션 계층만 추가한다.

---

## Task 1: 의존성 순서 정렬기 (M1)

**Files:**
- Create: `dependency_order.py`
- Test: `tests/test_dependency_order.py`

- [ ] **Step 1: Write the failing test**

```python
# tests/test_dependency_order.py
from dependency_order import order_by_dependency


def _assert_dependency_invariant(ordered, edges):
    """모든 엣지(caller, callee)에서 callee가 caller보다 먼저 와야 한다."""
    pos = {name: i for i, name in enumerate(ordered)}
    for caller, callee in edges:
        if caller in pos and callee in pos:
            assert pos[callee] < pos[caller], f"{callee} must precede {caller}"


def test_linear_chain():
    # A는 B에, B는 C에 의존 → 처리 순서: C, B, A
    nodes = {"A", "B", "C"}
    edges = [("A", "B"), ("B", "C")]
    ordered, cycles = order_by_dependency(nodes, edges)
    assert cycles == []
    assert ordered == ["C", "B", "A"]


def test_independent_nodes_sorted():
    # 의존 없는 노드는 모두 포함, 사전순 안정 정렬
    ordered, cycles = order_by_dependency({"X", "Y", "Z"}, [])
    assert ordered == ["X", "Y", "Z"]
    assert cycles == []


def test_diamond_invariant():
    # D→B, D→C, B→A, C→A : A가 가장 먼저
    nodes = {"A", "B", "C", "D"}
    edges = [("D", "B"), ("D", "C"), ("B", "A"), ("C", "A")]
    ordered, cycles = order_by_dependency(nodes, edges)
    assert cycles == []
    assert ordered[0] == "A"
    assert ordered[-1] == "D"
    _assert_dependency_invariant(ordered, edges)


def test_cycle_reported_not_ordered():
    # A↔B 순환 → 둘 다 cycle로, ordered엔 정상 노드만
    nodes = {"A", "B", "C"}
    edges = [("A", "B"), ("B", "A"), ("C", "A")]  # C는 A에 의존(순환 일부)
    ordered, cycles = order_by_dependency(nodes, edges)
    assert set(cycles) == {"A", "B", "C"}  # 순환에 막혀 아무도 in_degree 0 안 됨? → 아래 주석 참고
    assert ordered == []


def test_unknown_edge_endpoints_ignored():
    ordered, cycles = order_by_dependency({"A"}, [("A", "GHOST"), ("GHOST", "A")])
    assert ordered == ["A"]
    assert cycles == []
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `python -m pytest tests/test_dependency_order.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'dependency_order'`

- [ ] **Step 3: Write minimal implementation**

```python
# dependency_order.py
"""의존성 위상 정렬 — callee(피호출/피의존)가 caller보다 먼저 오도록 정렬한다."""
from collections import deque


def order_by_dependency(
    nodes: set[str],
    edges: list[tuple[str, str]],
) -> tuple[list[str], list[str]]:
    """
    edges: (caller, callee) — caller가 callee에 의존하므로 callee를 먼저 처리한다.
    반환: (ordered, cycle_nodes)
      - ordered: 의존성 순서 리스트. 순환에 속한 노드는 제외.
      - cycle_nodes: 순환 의존으로 정렬 불가한 노드(사전순).
    노드/엣지에 없는 식별자와 self-loop은 무시한다.
    """
    deps: dict[str, set[str]] = {n: set() for n in nodes}
    dependents: dict[str, set[str]] = {n: set() for n in nodes}
    for caller, callee in edges:
        if caller not in nodes or callee not in nodes or caller == callee:
            continue
        if callee not in deps[caller]:
            deps[caller].add(callee)
            dependents[callee].add(caller)

    in_degree = {n: len(deps[n]) for n in nodes}
    ready = deque(sorted(n for n in nodes if in_degree[n] == 0))
    ordered: list[str] = []
    while ready:
        n = ready.popleft()
        ordered.append(n)
        for dep in sorted(dependents[n]):
            in_degree[dep] -= 1
            if in_degree[dep] == 0:
                ready.append(dep)

    cycle_nodes = sorted(n for n in nodes if in_degree[n] > 0)
    return ordered, cycle_nodes
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `python -m pytest tests/test_dependency_order.py -v`
Expected: PASS (5 passed). 만약 `test_cycle_reported_not_ordered`에서 C가 ordered에 들어가면(=C가 A 외 의존 없음) 기대값을 `set(cycles) == {"A","B"}`, `ordered == ["C"]`로 정정하고 재확인. (C는 A에만 의존하는데 A가 순환에 막혀 영원히 ready 안 됨 → C도 cycle. 위 기대값이 맞다.)

- [ ] **Step 5: Commit**

```bash
git add dependency_order.py tests/test_dependency_order.py
git commit -m "feat(mvp): 의존성 위상 정렬기 (callee 우선) + 테스트"
```

---

## Task 2: 순차 적재 드라이버 (M2)

**Files:**
- Create: `ingest_driver.py`
- Test: `tests/test_ingest_driver.py`

순서대로 submit → **완료까지 대기** → 다음. 완료 대기가 핵심(다음 패키지 처리 전에 현재 패키지가 LightRAG에 적재돼야 callee 보장). 로직은 순수 함수 `run_ingestion`으로 두고 클라이언트를 주입(DI)해 테스트한다. 실제 HTTP는 얇은 `HttpRoboticsClient`로 분리(사내망 통합 시 사용).

- [ ] **Step 1: Write the failing test**

```python
# tests/test_ingest_driver.py
from ingest_driver import run_ingestion, IngestionReport


class FakeClient:
    """submit/wait 호출 순서를 기록하는 가짜 클라이언트.
    각 submit 직후 반드시 wait가 와야 함(순차성)을 검증한다."""
    def __init__(self, fail: set[str] | None = None):
        self.fail = fail or set()
        self.events: list[str] = []
        self._pending: str | None = None

    def submit(self, name: str, source: bytes, asset_type: str) -> str:
        assert self._pending is None, f"이전 작업({self._pending}) 미완료 상태에서 {name} 제출됨"
        self._pending = name
        self.events.append(f"submit:{name}")
        return f"job-{name}"

    def wait_until_done(self, job_id: str) -> str:
        name = job_id.removeprefix("job-")
        assert self._pending == name
        self._pending = None
        self.events.append(f"wait:{name}")
        return "failed" if name in self.fail else "completed"


def _sources(available):
    return lambda name: (b"SRC:" + name.encode()) if name in available else None


def test_submits_in_given_order_and_waits_each():
    client = FakeClient()
    ordered = ["C", "B", "A"]
    report = run_ingestion(ordered, client, _sources({"A", "B", "C"}))
    assert client.events == [
        "submit:C", "wait:C", "submit:B", "wait:B", "submit:A", "wait:A",
    ]
    assert report.completed == ["C", "B", "A"]
    assert report.failed == []
    assert report.skipped_missing == []


def test_missing_source_skipped():
    client = FakeClient()
    report = run_ingestion(["A", "B"], client, _sources({"A"}))
    assert report.skipped_missing == ["B"]
    assert report.completed == ["A"]
    assert client.events == ["submit:A", "wait:A"]


def test_failed_status_recorded():
    client = FakeClient(fail={"B"})
    report = run_ingestion(["A", "B"], client, _sources({"A", "B"}))
    assert report.completed == ["A"]
    assert report.failed == [("B", "failed")]
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `python -m pytest tests/test_ingest_driver.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'ingest_driver'`

- [ ] **Step 3: Write minimal implementation**

```python
# ingest_driver.py
"""의존성 순서대로 패키지를 Robotics에 순차 투입하는 드라이버.

run_ingestion: 순수 오케스트레이션(클라이언트 주입). 단위 테스트 대상.
HttpRoboticsClient: 실제 /jobs 호출. 사내망 통합 시 사용(단위 테스트 제외).
"""
import time
from dataclasses import dataclass, field
from typing import Callable, Protocol


class RoboticsClient(Protocol):
    def submit(self, name: str, source: bytes, asset_type: str) -> str: ...
    def wait_until_done(self, job_id: str) -> str: ...


@dataclass
class IngestionReport:
    completed: list[str] = field(default_factory=list)
    failed: list[tuple[str, str]] = field(default_factory=list)
    skipped_missing: list[str] = field(default_factory=list)


def run_ingestion(
    ordered: list[str],
    client: RoboticsClient,
    source_resolver: Callable[[str], bytes | None],
    asset_type: str = "plsql",
) -> IngestionReport:
    """ordered 순서대로: 소스 조회 → submit → 완료 대기 → 다음.
    완료 대기로 callee가 다음 패키지 처리 전에 LightRAG에 적재됨을 보장한다."""
    report = IngestionReport()
    for name in ordered:
        source = source_resolver(name)
        if source is None:
            report.skipped_missing.append(name)
            continue
        job_id = client.submit(name, source, asset_type)
        status = client.wait_until_done(job_id)
        if status == "completed":
            report.completed.append(name)
        else:
            report.failed.append((name, status))
    return report


class HttpRoboticsClient:
    """실제 Robotics /jobs 호출. (사내망 통합용 — 단위 테스트 대상 아님)"""

    def __init__(self, base_url: str, poll_interval: float = 2.0, timeout: float = 600.0):
        import httpx
        self._http = httpx.Client(base_url=base_url, timeout=30.0)
        self._poll = poll_interval
        self._timeout = timeout

    def submit(self, name: str, source: bytes, asset_type: str) -> str:
        resp = self._http.post(
            "/jobs",
            files={"file": (f"{name}.sql", source)},
            data={"asset_type": asset_type, "requested_by": "ingest_driver"},
        )
        resp.raise_for_status()
        return resp.json()["job_id"]

    def wait_until_done(self, job_id: str) -> str:
        deadline = time.monotonic() + self._timeout
        while time.monotonic() < deadline:
            resp = self._http.get(f"/jobs/{job_id}")
            resp.raise_for_status()
            status = resp.json()["status"]
            if status in ("completed", "failed"):
                return status
            time.sleep(self._poll)
        return "timeout"

    def close(self) -> None:
        self._http.close()
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `python -m pytest tests/test_ingest_driver.py -v`
Expected: PASS (3 passed). `HttpRoboticsClient`는 테스트하지 않음(사내망 통합 단계).

- [ ] **Step 5: Commit**

```bash
git add ingest_driver.py tests/test_ingest_driver.py
git commit -m "feat(mvp): 순차 적재 드라이버(run_ingestion DI) + HTTP 클라이언트"
```

---

## Task 3: 현상 요약 + 관찰 템플릿 (M3)

**Files:**
- Create: `observation.py`
- Create: `docs/observations/TEMPLATE.md`
- Test: `tests/test_observation.py`

적재 결과(`IngestionReport`)에서 통과율·카운트를 집계한다. caller 정보 빈약 여부 등 **정성 판단은 수동**(템플릿)으로 — MVP는 자동 판정하지 않는다(현상 파악이 목적).

- [ ] **Step 1: Write the failing test**

```python
# tests/test_observation.py
from ingest_driver import IngestionReport
from observation import summarize, Observation


def test_summarize_counts_and_pass_rate():
    report = IngestionReport(
        completed=["A", "B", "C"],
        failed=[("D", "failed")],
        skipped_missing=["E"],
    )
    obs = summarize(report)
    assert obs == Observation(total=5, completed=3, failed=1, skipped=1)
    assert obs.pass_rate == 0.6  # 3/5


def test_pass_rate_zero_when_empty():
    obs = summarize(IngestionReport())
    assert obs.total == 0
    assert obs.pass_rate == 0.0
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `python -m pytest tests/test_observation.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'observation'`

- [ ] **Step 3: Write minimal implementation**

```python
# observation.py
"""적재 결과 현상 요약. 정성 판단(빈약 지점 등)은 수동 관찰(docs/observations) 몫."""
from dataclasses import dataclass

from ingest_driver import IngestionReport


@dataclass(frozen=True)
class Observation:
    total: int
    completed: int
    failed: int
    skipped: int

    @property
    def pass_rate(self) -> float:
        return self.completed / self.total if self.total else 0.0


def summarize(report: IngestionReport) -> Observation:
    completed = len(report.completed)
    failed = len(report.failed)
    skipped = len(report.skipped_missing)
    return Observation(
        total=completed + failed + skipped,
        completed=completed,
        failed=failed,
        skipped=skipped,
    )
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `python -m pytest tests/test_observation.py -v`
Expected: PASS (2 passed)

- [ ] **Step 5: Create observation template**

```markdown
<!-- docs/observations/TEMPLATE.md -->
# 역문서 현상 관찰 — YYYY-MM-DD

> 대상: (패키지 수 / 서브셋 범위)
> 적재 환경: (사내망 / 로컬)

## 정량 (observation.summarize)
- total / completed / failed / skipped:
- pass_rate:

## 정성 (수동)
- 잘 되는 것 (callee 맥락 등):
- 빈약한 것 (특히 "누가 호출하나" caller 정보):
- 기준1·2 실패 패턴:
- 의외의 현상:

## 판단
- deferred 항목 중 도입 근거가 생긴 것:
- 다음 액션:
```

- [ ] **Step 6: Commit**

```bash
git add observation.py tests/test_observation.py docs/observations/TEMPLATE.md
git commit -m "feat(mvp): 현상 요약 집계 + 관찰 템플릿"
```

---

## Task 4: 전체 회귀 확인

**Files:** (없음 — 검증만)

- [ ] **Step 1: Run full suite**

Run: `python -m pytest -q`
Expected: 신규 10개 테스트 통과. 기존 결과 유지(범위밖 사전 실패 2건 제외 — `test_file_too_large`, `test_check1_fail_missing_identifier`는 본 plan 범위 아님).

- [ ] **Step 2: Commit (필요 시 없음)**

신규 파일은 Task별로 이미 커밋됨. 추가 변경 없으면 생략.

---

## 사내망 실행 절차 (코드 완성 후, 본 PC 범위 밖)

1. 의존성 데이터(`03_Phase1_데이터준비`, DBA_DEPENDENCIES 산출)를 `(caller, callee)` 엣지 리스트로 로드 → `order_by_dependency`.
2. 패키지 바디 파일 디렉토리 기준 `source_resolver` 구성.
3. `HttpRoboticsClient(base_url=운영 Robotics)` + `run_ingestion` 실행.
4. `summarize` + `docs/observations/TEMPLATE.md` 복사해 현상 1건 기록.

---

## Self-Review

- **Spec coverage:** MVP 설계 §3 IN 항목 — 의존성 순서 정렬(Task1)/순차 적재(Task2)/검증 기준1·2 재사용(기존 엔진, 수정 없음)/현상 관찰(Task3) 모두 태스크 존재. OUT 항목(Outer/파킹/graph_epoch 등)은 의도적으로 미포함 ✓
- **Placeholder scan:** 모든 코드 스텝에 완전한 코드 포함, TBD/TODO 없음 ✓
- **Type consistency:** `IngestionReport`(completed/failed/skipped_missing) 필드를 Task2 정의 → Task3에서 동일 사용. `RoboticsClient.submit/wait_until_done` 시그니처 Task2 내 일관 ✓
- **사내망 제약:** 단위 테스트는 실데이터 무관(mock/FakeClient), HttpRoboticsClient는 테스트 제외 명시 ✓
